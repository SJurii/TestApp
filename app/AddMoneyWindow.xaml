<Window x:Class="app.AddMoney"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:app"
        mc:Ignorable="d"
        Title="AddMoney" Height="450" Width="800">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Код валюты (например, USD):" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox Text="{Binding CharCode, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Название (например, Доллар США):" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Курс к рублю:" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,20" Padding="5"/>

            <Button Content="Сохранить и закрыть" 
                    Command="{Binding SaveCommand}" 
                    Height="35" Background="LightGreen" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
